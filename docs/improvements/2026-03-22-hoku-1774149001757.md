# Mejora — 2026-03-22

**Agente:** hoku
**Tarea:** Crea un demo funcional de la integración con Hotjar. Genera el código HTML completo con estilos inline para mostrar cómo se vería el dashboard de Hotjar integrado en Smart Connection.

## Archivos generados
- `src/components/HotjarDashboard.tsx`
- `src/styles/HotjarDashboard.css`
- `src/utils/HotjarUtils.ts`

🐾 HOKU — Ejecutando 3 agentes en paralelo...

✅ Groq respondió
✅ Claude respondió
✅ Gemini respondió

📊 Sintetizando respuestas...

⚠️ Síntesis no disponible (rate limit). Mostrando respuestas individuales:


── GROQ ──
Error: 429

── CLAUDE ──
```tsx filename="src/components/HotjarDashboard.tsx"
import React, { useEffect, useState } from 'react';

interface HotjarDashboardProps {
  // No hay props necesarios para este componente
}

const HotjarDashboard: React.FC<HotjarDashboardProps> = () => {
  const [hotjarLoaded, setHotjarLoaded] = useState(false);

  useEffect(() => {
    const script = document.createElement('script');
    script.src = 'https://static.hotjar.com/c/hotjar-'.concat('ID_DE_HOTJAR', '.js?sv=');
    script.async = true;
    document.body.appendChild(script);

    script.onload = () => {
      setHotjarLoaded(true);
    };
  }, []);

  return (
    <div style={{
      width: '100%',
      height: '100vh',
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
    }}>
      {hotjarLoaded ? (
        <iframe
          src={`https://app.hotjar.com/dashboard/${'ID_DE_HOTJAR'}`}
          frameBorder="0"
          width="100%"
          height="100%"
          style={{
            border: 'none',
          }}
        />
      ) : (
        <p>Cargando Hotjar...</p>
      )}
    </div>
  );
};

export default HotjarDashboard;
```

```css filename="src/styles/HotjarDashboard.css"
/* No hay estilos necesarios para este componente */
```

```ts filename="src/utils/HotjarUtils.ts"
// No hay utilidades necesarias para este componente
```

Recuerda reemplazar `'ID_DE_HOTJAR'` con el ID real de tu cuenta de Hotjar. Este código crea un componente de React que carga el dashboard de Hotjar en un iframe y lo muestra en la pantalla. El componente utiliza el hook `useEffect` para cargar el script de Hotjar y el estado `hotjarLoaded` para mostrar un mensaje de carga mientras se carga el script. Una vez cargado el script, se muestra el iframe con el dashboard de Hotjar.

── GEMINI ──
Error: 429

